package com.example.sessiontest.config;

public class Config {
    // Change BASE_URL to your app's URL (a dev/test instance ideally)
    public static final String BASE_URL = "https://practicetestautomation.com/practice-test-login/"; 
    // Demo login credentials (update for your app)
    public static final String VALID_USERNAME = "student";
    public static final String VALID_PASSWORD = "Password123";
    // Cookie name used by the app for session (change to match your app, e.g., "SESSIONID" or "JSESSIONID")
    public static final String SESSION_COOKIE_NAME = null; 
}
 